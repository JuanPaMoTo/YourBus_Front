export default function Notificaciones() {
  // Este componente es la base para conectar retrasos e incidentes
  // (HU07 y HU08). Cuando el backend exponga websockets o polling,
  // se integra aquí.
  return (
    <section className="page">
      <h1>Notificaciones y alertas</h1>
      <p>Aún no hay notificaciones activas.</p>
    </section>
  );
}
